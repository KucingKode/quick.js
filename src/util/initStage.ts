function initStage(theme: string) {
    const struct = `
        <style>
        </style>

    `

    document.querySelector('body').innerHTML = ''
    document.querySelector('body').innerHTML = struct
}

export default initStage